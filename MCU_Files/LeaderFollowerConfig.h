#ifndef LEADER_FOLLOWER_CONFIG_H
#define LEADER_FOLLOWER_CONFIG_H

// ══════════════════════════════════════════════════════════════════════════════
// LeaderFollowerConfig.h - كل إعدادات نظام القائد والتابع
// ══════════════════════════════════════════════════════════════════════════════

// ── إعدادات التوقيت ──────────────────────────────────────────────────────────
#define LF_BROADCAST_INTERVAL_MS  100    // كم مرة يرسل القائد الحزمة (ms) //✅add

// ── إعدادات المسافة والأمان ──────────────────────────────────────────────────
#define LF_TARGET_CM              25     // المسافة المثالية بين القائد والتابع (cm) //✅add
#define LF_DEADBAND_CM            5      // المنطقة الميتة (±5 سم) //✅add
#define LF_TOO_CLOSE_CM           12     // مسافة الطوارئ (توقف فوري) //✅add
#define LF_LOST_TIMEOUT_MS        800    // مهلة فقدان الإشارة (ms) //✅add

// ── إعدادات السرعة ───────────────────────────────────────────────────────────
#define LF_LEADER_SPEED           160    // سرعة القائد (PWM) //✅add
#define LF_FOLLOWER_SPEED         150    // سرعة التابع (PWM) //✅add

// ── إعدادات الـ Serial ───────────────────────────────────────────────────────
#define LF_SERIAL_BUF_SIZE        48     // حجم buffer استقبال التابع //✅add

// ── معرفات الأدوار ───────────────────────────────────────────────────────────
#define ROLE_LEADER               1      // ✅add
#define ROLE_FOLLOWER             2      // ✅add

// ── اختيار الدور (للكود الحالي - أنت القائد) ─────────────────────────────────
#define VEHICLE_ROLE  ROLE_LEADER        // ✅add (أنت القائد)

#endif  // LEADER_FOLLOWER_CONFIG_H